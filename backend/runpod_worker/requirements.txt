# PersonaPlex Pod Server Dependencies
# For dedicated GPU pod with WebSocket streaming

# Web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.0.0

# ML / Inference - pin numpy for moshi compatibility
torch>=2.2.0
accelerate>=0.25.0
safetensors
sentencepiece
protobuf
numpy>=1.26,<2.2
scipy
soundfile
huggingface_hub
sphn

# Optional: RunPod SDK (for fallback serverless mode)
runpod>=1.6.0

# Moshi/PersonaPlex - installed from git in Dockerfile
